<%# Conditionally set remote: true. Also, passing model name as data attribute %>
<% modal ||= false %>
<% remote = modal ? true : false %>
<%= form_for(@client, remote: remote, html: {role: :form, 'data-model' => 'client'}) do |f| %>
  <% if @client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% @client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label "Identificacion", class: 'control-label'%><br>
    <%= f.text_field :identification,class: 'form-control' %>
  </div>
  <div class="field form-group">
    <%= f.label "Nombre", class: 'control-label' %><br>
    <%= f.text_field :name,class: 'form-control' %>
  </div>
  <div class="field form-group">
    <%= f.label "Direccion", class: 'control-label'%><br>
    <%= f.text_field :direction,class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label "Telefono" %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label "Email" %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label "Fecha Cumpleaños"%><br>
    <%= f.date_select :birthdate  %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar", {:class=>"btn btn-default"} %>
  </div>
<% end %>
