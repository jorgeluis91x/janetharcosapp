<%= form_for(@state_product) do |f| %>
  <% if @state_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state_product.errors.count, "error") %> prohibited this state_product from being saved:</h2>

      <ul>
      <% @state_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
